## .NET
bin/
obj/
*.user
.vs/

## Build output
[Dd]ebug/
[Rr]elease/
artifacts/

## Secrets — never commit App private keys, tokens, or local overrides
*.pem
appsettings.*.Local.json
appsettings.Local.json
.env
*.env

## Azure Functions local settings (may contain secrets)
local.settings.json

## Local Bicep params (may contain secrets)
.azure/*.local.bicepparam

## Local runbooks — may contain subscription ids, resource names, deploy logs
*.local.md

## Local tooling
.claude/settings.local.json
.playwright-mcp/

## Support-ticket drafts — contain internal org/target/service-account ids
tickets/

## Local git worktrees
.worktrees/
