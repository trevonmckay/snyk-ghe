# Normalize line endings: store as LF in the repo, check out native on each platform.
* text=auto

# Files that must stay LF regardless of platform (run inside Linux containers).
*.sh text eol=lf
Dockerfile text eol=lf
